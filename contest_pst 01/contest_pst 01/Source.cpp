#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <string>
#include <unordered_set>
#include <math.h>
#include <bitset>
typedef long long ll;
using namespace std;
int const N = 1e6+6;
int T, n, k;
char s[N];
int main(){

	scanf("%d %d", &n, &k);
	scanf("%s", s);
	int i = 0, j = 0, boys = 0, ways = 0;
	for (i; s[i]; ++i)
	{
		for (int j = i + 1; s[j]; ++j){

		}
	}
	return 0;
}